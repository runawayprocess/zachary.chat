/** @type {import('next').NextConfig} */
const nextConfig = {
  // Enable static export if needed for simple hosting
  // output: 'export',
  // images: { unoptimized: true },
};

module.exports = nextConfig;
